import { NextFunction, Request, Response } from "express";

const validateEmailMiddlewares = (
    req: Request,
    res: Response,
    next: NextFunction
) => {};

export { validateEmailMiddlewares };
